# Flask CRUD Application Requirements
# ===================================
# 
# This file contains all the Python packages required to run the Flask CRUD application.
# Install these packages using: pip install -r requirements.txt

# Core Flask framework
Flask==2.3.3

# Database ORM for Flask
Flask-SQLAlchemy==3.1.1

# Additional Flask extensions (optional but recommended)
Flask-WTF==1.2.2          # For form handling and CSRF protection
WTForms==3.2.1            # Form validation library

# Development dependencies (optional)
python-dotenv==1.2.1      # For environment variable management

# Note: SQLite is included with Python by default, so no additional database driver is needed
